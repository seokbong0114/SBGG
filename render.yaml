services:
  - type: web
    name: sbgg
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: RIOT_API_KEY
        sync: false
